name: "Step 4 — 소액주주 데이터 수집"

on:
  workflow_dispatch:
  schedule:
    # 매일 UTC 01:30 (한국 10:30) — Step 3 미완료 시 자동 스킵됨
    - cron: "30 1 * * *"

jobs:
  collect:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Create .env
        run: echo "DART_API_KEY=${{ secrets.DART_API_KEY }}" > .env

      - name: Download listed_corps.csv (Step 1 artifact)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p data/raw
          ARTIFACT_ID=$(gh api \
            "repos/${{ github.repository }}/actions/artifacts?name=step1-corp-list&per_page=1" \
            --jq '.artifacts[0].id // empty')
          if [ -z "$ARTIFACT_ID" ]; then
            echo "::error::Step 1 아티팩트가 없습니다."
            exit 1
          fi
          gh api "repos/${{ github.repository }}/actions/artifacts/$ARTIFACT_ID/zip" > /tmp/step1.zip
          unzip -o /tmp/step1.zip -d data/raw/

      # ── Step 3 완료 여부 확인 ────────────────────────────────────────────
      - name: Check Step 3 completion
        id: check_step3
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARTIFACT_ID=$(gh api \
            "repos/${{ github.repository }}/actions/artifacts?name=step3-treasury-data&per_page=1" \
            --jq '.artifacts[0].id // empty')

          if [ -z "$ARTIFACT_ID" ]; then
            echo "Step 3 artifact 없음 → 스킵"
            echo "skip=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          gh api "repos/${{ github.repository }}/actions/artifacts/$ARTIFACT_ID/zip" > /tmp/step3_check.zip
          mkdir -p /tmp/step3_check
          unzip -o /tmp/step3_check.zip -d /tmp/step3_check/

          python - <<'PYEOF'
          import json, sys
          from pathlib import Path

          cp_path = Path("/tmp/step3_check/treasury_checkpoint.json")
          corps_path = Path("data/raw/listed_corps.csv")

          if not cp_path.exists():
              print("체크포인트 없음 → 스킵")
              Path("/tmp/step3_done").write_text("false")
              sys.exit(0)

          cp = json.loads(cp_path.read_text())
          done = sum(len(v) for v in cp.get("completed", {}).values())
          total_corps = sum(1 for _ in open(corps_path)) - 1
          total = total_corps * 11

          pct = done / total * 100 if total else 0
          print(f"Step 3 진행: {done:,}/{total:,} ({pct:.1f}%)")

          if done >= total:
              print("Step 3 완료 ✓ → Step 4 진행")
              Path("/tmp/step3_done").write_text("true")
          else:
              remaining = total - done
              print(f"Step 3 미완료 (잔여 {remaining:,}건) → Step 4 스킵")
              Path("/tmp/step3_done").write_text("false")
          PYEOF

          STEP3_DONE=$(cat /tmp/step3_done)
          echo "skip=$([ "$STEP3_DONE" = "true" ] && echo false || echo true)" >> $GITHUB_OUTPUT

      - name: Skip notice
        if: steps.check_step3.outputs.skip == 'true'
        run: |
          echo "⏭️  Step 3가 아직 완료되지 않았습니다. Step 4를 스킵합니다." | tee -a $GITHUB_STEP_SUMMARY
          echo "Step 3가 모두 완료된 후 Step 4가 자동 시작됩니다." >> $GITHUB_STEP_SUMMARY

      # ── 이후 스텝은 Step 3 완료 시에만 실행 ────────────────────────────

      - name: Restore shared daily counter
        if: steps.check_step3.outputs.skip == 'false'
        run: |
          cp /tmp/step3_check/.api_daily_counter.json data/raw/ 2>/dev/null || true

      - name: Restore previous minority data
        if: steps.check_step3.outputs.skip == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARTIFACT_ID=$(gh api \
            "repos/${{ github.repository }}/actions/artifacts?name=step4-minority-data&per_page=1" \
            --jq '.artifacts[0].id // empty')
          if [ -n "$ARTIFACT_ID" ]; then
            echo "이전 minority 데이터 복원 중..."
            gh api "repos/${{ github.repository }}/actions/artifacts/$ARTIFACT_ID/zip" > /tmp/step4.zip
            unzip -o /tmp/step4.zip -d data/raw/
          else
            echo "첫 실행"
          fi

      - name: Run Step 4
        if: steps.check_step3.outputs.skip == 'false'
        run: python src/04_collect_minority.py

      - name: Upload artifact
        if: steps.check_step3.outputs.skip == 'false' && always()
        uses: actions/upload-artifact@v4
        with:
          name: step4-minority-data
          path: |
            data/raw/minority_raw.csv
            data/raw/minority_checkpoint.json
            data/raw/.api_daily_counter.json
          retention-days: 90
          overwrite: true

      - name: Summary
        if: steps.check_step3.outputs.skip == 'false' && always()
        run: |
          python - <<'EOF'
          import json, os
          summary = []
          try:
              cp = json.load(open("data/raw/minority_checkpoint.json"))
              done = sum(len(v) for v in cp.get("completed", {}).values())
              summary.append(f"완료: **{done:,}건**")
          except: pass
          try:
              ct = json.load(open("data/raw/.api_daily_counter.json"))
              summary.append(f"오늘 API 호출: **{ct['calls']:,}/10,000**")
          except: pass
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a") as f:
              f.write("## Step 4 결과\n")
              for s in summary:
                  f.write(f"- {s}\n")
          EOF
